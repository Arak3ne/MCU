-- Temporarily disable RLS to allow the seed script to run
-- Run this in your Supabase SQL editor before running the seed script:
ALTER TABLE champions DISABLE ROW LEVEL SECURITY;

-- ... Run the script ...

-- Then re-enable it:
-- ALTER TABLE champions ENABLE ROW LEVEL SECURITY;
